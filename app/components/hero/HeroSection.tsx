import Header from "./Header";
import { ScrollScene } from "./scene";

export default function HeroSection() {
  return (
    <section className="relative w-full">
      <Header />
      <ScrollScene />
    </section>
  );
}
